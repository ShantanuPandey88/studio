
export type Booking = {
  id: string;
  deskId: string;
  date: Date;
  userId: string;
  userName: string;
};

export type Holiday = {
  id: string;
  date: Date;
  name: string;
};

export type Desk = {
  id: string;
}

    