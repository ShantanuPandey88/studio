# Email configuration for password resets
# You can use a service like SendGrid, Brevo, or a standard SMTP provider.
# For testing with Gmail, use smtp.gmail.com, port 465, and your app password.
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM="SeatServe <no-reply@example.com>"
